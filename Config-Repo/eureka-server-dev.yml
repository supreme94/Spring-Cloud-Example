eureka:
  instance:
    hostname: localhost
    metadata_map:
      -:
        ${spring.application.name}:${spring.application.instance_id:${randpm.value}}
  client:
    register-with-eureka: false
    fetch_registry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/